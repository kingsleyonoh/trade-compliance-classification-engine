# sqlx offline metadata

This directory is reserved for committed sqlx query metadata. Phase 0 enables
`SQLX_OFFLINE=true`; query metadata will be generated when compile-time checked
queries are introduced with migrations.
